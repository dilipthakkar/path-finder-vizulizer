{
  "name": "path-finder-vizulizer",
  "version": "1.0.0",
  "description": "path finding algorithm visulizer",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild" : "cd client && npm install && npm run build"
  },
  "author": "dilip thakkar",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "engines": {
    "node" : "15.x",
    "npm" : "7.x"
  }
}
